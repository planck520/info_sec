/* 设置页 + LLM 面板逻辑 — 成员 C 实现 */

AppState.registerPage('settings', {
  init: function () {
    // TODO: 成员 C — 渲染 IDA 配置 + LLM 配置 + 关于
  },
  destroy: function () {
    // TODO: 成员 C — 清理事件监听
  },
});
